import { Card, gray } from '@nextui-org/react';
import styles from './section.module.css';

const Section = (props) => {
  return <div className={styles.section} {...props} />;
};

export default Section;
